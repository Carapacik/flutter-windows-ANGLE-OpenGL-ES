name: flutter_windows_angle_opengl_es_example
description: Demonstrates how to use the flutter_windows_angle_opengl_es plugin.
publish_to: 'none'
version: 1.1.0+1
environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_windows_angle_opengl_es:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
